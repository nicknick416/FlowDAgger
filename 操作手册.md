# FlowDAgger ARX 训练操作手册

更新时间：2026-09-03

本手册只覆盖当前 robot-ubuntu 上的 **ARX 插电梯针** campaign，不涉及 MetaWorld 仿真。
默认走 **在线 DAgger**：人接管成功后，回合结束自动 BC，并切换 `ACTIVE`。

项目目录：`/home/ubuntu/yzy/FlowDAgger`

路径、prompt、端口、训练超参都写在 `/home/ubuntu/yzy/FlowDAgger/arx_campaign.yaml`。改这个文件并重启服务即可，不必改 Python。新开一轮训练只改 `campaign_id`（例如 `..._v6`），`output_root` 默认是 `{runs_root}/{campaign_id}`。

---

## 1. 固定配置


| 项           | 路径 / 值                                          |
| ----------- | ----------------------------------------------- |
| 代码          | `/home/ubuntu/yzy/FlowDAgger`                   |
| 配置文件        | `/home/ubuntu/yzy/FlowDAgger/arx_campaign.yaml` |
| Python      | `/home/ubuntu/openpi/.venv/bin/python`          |
| OpenPI      | 见 yaml 的 `openpi_root` / `openpi_config`        |
| 冻结基座        | 见 yaml 的 `base_checkpoint`                      |
| Campaign 输出 | 见 yaml：默认 `{runs_root}/{campaign_id}`           |
| 服务端口        | 见 yaml 的 `addr`（默认 `tcp://*:5557`）              |
| 协议          | v3                                              |
| Prompt      | 见 yaml 的 `default_prompt`                       |


旧 campaign（`arx_connect_elevator_pins_0829_332`，以及 `..._base_retrain_v2` / `..._v3` / `..._v4` / 当前正在用的目录）**不要读、不要删、不要把 episode 混进新** `output_root`。新目录没有 ACTIVE，从 bootstrap 开始。Steering 预测完整 50×32 noise（不再做 DCT）；z-score 要等 **3 次成功回合** 才冻结。换了基座后，旧反演结果不能复用。

也可以启动时临时换配置文件，不必改默认 yaml：

```bash
./run_arx_flowdagger_server.sh --config-file /path/to/other.yaml
```

命令行 `--output-root` / `--checkpoint` / `--addr` 仍可覆盖 yaml 里的对应项。环境变量 `FLOWDAGGER_CONFIG` 也能指定默认 yaml 路径。

不要跑 `run_arx_prepare_bootstrap_manifest.sh`。当前实现已经去掉审核 manifest / 降幅门禁。

---



## 2. 现在应该怎么训（推荐）

日常训练只做一件事：起完整 FlowDAgger 服务，客户端按阶段采集。**接管成功的回合会在** `episode_end` **后自动训练。**

### 2.1 启动服务

需要 GPU。一个终端：

```bash
cd /home/ubuntu/yzy/FlowDAgger
./run_arx_flowdagger_server.sh
```

启动后看 yaml 里的 `output_root`（默认 `{runs_root}/{campaign_id}`）：

```bash
# 把 CAMPAIGN 换成 arx_campaign.yaml 里实际的 output_root
CAMPAIGN=/home/ubuntu/yzy/flowdagger_runs/<campaign_id>
tail -f ${CAMPAIGN}/logs/service.log
```

确认加载的是新 `campaign_id`，监听 yaml 里的端口。日志会打印 `loaded campaign config ... campaign_id=... output_root=...`。**改 yaml / 改代码或换基座后必须重启服务**。若当前有 BC 在跑，等它结束再重启。

### 2.2 客户端阶段


| 阶段            | `run_stage`     | `shadow_mode` | 谁在控               | 会不会训                |
| ------------- | --------------- | ------------- | ----------------- | ------------------- |
| 从基座开始         | `bootstrap`     | `true`        | 基座（还没有 steering）  | 接管成功会训，写出第一版 ACTIVE |
| 用 steering 继续 | `closed_loop`   | `false`       | steering          | 接管成功会训，ACTIVE +1    |
| 只看不改          | `shadow`        | `true`        | 基座执行，steering 只对比 | 否                   |
| 纯示教归档         | `demonstration` | `true`        | 人                 | 否（要另跑离线脚本才训）        |


**第一次、还没有** `steering_checkpoints/ACTIVE`**：**

1. 客户端 `run_stage=bootstrap`、`shadow_mode=true`、`protocol_version=3`。
2. 先让模型跑，出错再接管。典型节奏是 **策略 → 人工 → 策略**。
3. 人工段必须有真实位移，不能只按按钮停住。回合成功结束且至少有一段连续 expert，服务端会排队 BC。
4. 等 `train_status` 变成 `succeeded`，再改客户端。

**已经有 ACTIVE 之后：**

1. 重启或继续用同一服务均可；启动时会读 `steering_checkpoints/ACTIVE`。
2. 客户端改成 `run_stage=closed_loop`、`shadow_mode=false`。
3. 继续「模型跑、人接管、再交回模型」。每次接管成功仍会自动更新。

在线更新超参（不用改命令）：100 steps，batch 64，三路按 **4:4:2** 混合：

- **intervention**：这局和历史接管成功里的人工段
- **autonomous**：成功回合里策略自己跑的部分（不必整段都是自主成功）
- **demonstration**：`/home/ubuntu/yzy/flowdagger_runs/raw_data/arx_connect_elevator_pins_raw_data` 里按时长筛出的 30 条最短专家示教

缺某一路时按剩余权重重分配。学习率 `1e-4`，steering 输出 50×32 完整 noise。归一化在前 3 次成功回合会按当前训练集重拟合，第 3 次起冻结。

反演结果会落到磁盘：同一窗口、同一冻结基座只算一次。campaign 回合写在该 episode 的 `inversion_cache/`；30 条示教写在示教目录下的 `inversion_cache/<episode_id>/`，不写进原始 raw 数据。缓存的是**原始** 50×32 系数和 VLM feature，z-score 仍在训练时现算。换基座或改 inversion 阈值会自动作废旧缓存。**第一次 V5 训练会反演全部示教窗口，之后只反演新窗口。**



### 2.3 采集时注意（和训练能不能触发直接相关）

1. **人要真的动起来。** 切换按钮后的静止 Pause 不会写入；如果整段接管都没有超过约 0.5 mm 的双臂位移，就不会留下连续 expert，`training_queued=false`。
2. **人工结束后把控制交回策略再跑一会儿。** 人工不足 50 步时，训练窗口用后面的策略 `predicted_actions` 补齐，而不是最后一帧冻住。这是延后补齐，后续策略轨迹可以模仿。回合在人工段直接结束时，只能退回最后一帧补齐。
3. 同一段接管里，中途停一下（例如插针对准）会保留；只有段首、段尾的切换延迟会被丢掉。
4. 成功才更新：`task_outcome=success`，且 `intervention_count > 0`，且裁完 Pause 后仍有连续 expert。失败 / 中止不训。纯自主成功会进入 autonomous 池，等下一次接管成功时一起混进去。
5. 训练跑着时不能开新 episode。等本轮 BC 结束再开始下一回合。

---



## 3. 可选：用示教离线训出第一版

只有在你想先囤示教、再一次性出 ACTIVE 时才走这条路。日常 DAgger 不必先做。

### 3.1 录示教

另开服务（不要和在线服务抢同一端口）：

```bash
cd /home/ubuntu/yzy/FlowDAgger
./run_arx_flowdagger_server.sh --record-only
```

客户端：`run_stage=demonstration`、`shadow_mode=true`。全程人工也可以，但必须有相邻 expert 帧和三路相机。成功回合会进 `episodes/`，**当时不会 BC**。

### 3.2 离线训练

确认没有在线服务占着同一 `output-root` 的 ACTIVE，然后：

```bash
cd /home/ubuntu/yzy/FlowDAgger
./run_arx_offline_train.sh
```

它只吃本 campaign 里 `run_stage=demonstration` 的成功示教。通过后写入：

```text
.../steering_checkpoints/version_XXXXXX/
.../steering_checkpoints/ACTIVE
.../offline_training/<时间戳>/offline_gate.json
```

然后再起 **不带** `--record-only` 的完整服务，客户端用 `closed_loop`。

---



## 4. 怎么确认这轮训上了



### 4.1 当场

客户端 `episode_end` 的返回里应有 `"training_queued": true`。

随后查训练状态（`cmd=train_status` 或看服务日志）：

- `state=running`：正在反演 + BC，可能要几分钟。
- `state=succeeded`：已写成新 version，并切换 ACTIVE。
- `state=failed`：本回合作废，仍用上一版。看 `service.log` 和该 episode 目录下的报错。

日志里会出现类似：

```text
[FlowDAgger][   xx.x s] 阶段 1/4 数据准备: episodes=... windows=...
[FlowDAgger][   xx.x s] 阶段 2/4 BC 反演开始: windows=... cache_hits=... invert=...
```

`cache_hits` 接近窗口总数、`invert` 接近 0 时，说明在走缓存。

### 4.2 落盘

```bash
# 与 arx_campaign.yaml 中的 output_root 一致
CAMPAIGN=/home/ubuntu/yzy/flowdagger_runs/<campaign_id>

cat ${CAMPAIGN}/steering_checkpoints/ACTIVE
ls ${CAMPAIGN}/steering_checkpoints/
ls ${CAMPAIGN}/episodes/ | tail
```

每个成功更新的回合目录里还有 `inversion_report.json`、`training_metrics.json`。关注：

- `num_windows` / `acceptance_rate`：窗口是否被反演接受
- `E_full` / `E_repr` / `E_actor`：完整 inversion、部署表示、steering 预测的动作重建误差
- `train_padded_windows`：仍用最后一帧补齐的窗口数。后面有策略续跑时，这个数应下降
- `pad_source`（窗口 info）：`predicted_actions` 或 `policy_states` 表示已用策略补齐



### 4.3 健康检查

客户端连上后先 `health`，确认：

- `protocol_version=3`
- `campaign_id` 与 `arx_campaign.yaml` 一致
- `runtime_mode=flowdagger`（在线）或 `base_record`（示教）
- `policy_version` 与预期 ACTIVE 一致
- `action_horizon=50`，`action_dim=20`

---

## 5. 不要做的事

- 不要把实机客户端连到 `--protocol-only`（默认若改端口，例如 `5558`）。那是零动作协议测试。
- 不要在训练 `running` 时开下一局。
- 不要把旧 campaign 的 episode 拷进当前 `output-root`。
- 不要改基座 checkpoint；steering 只写 `steering_checkpoints/`。
- 不要指望「按一下接管又立刻松开、人没动」会触发训练。

---

